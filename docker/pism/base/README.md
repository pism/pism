This Docker image is used to build PISM images uploaded to the GitHub Container Registry.

The same `Dockerfile` can be used to build the "base" image (PISM's prerequisite libraries
and build tools) and the "runtime" image (PISM's prerequisite libraries, but without build
tools).
