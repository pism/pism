VPATH += :../src/coupler/

exec_default += ../bin/pcc

pcc_sources += pccoupler.cc pPDDcoupler.cc pcc.cc

PCC_OBJS := $(pcc_sources:.cc=.o)

other_sources += $(pcc_sources)

../bin/pcc : ${PCC_OBJS} ${LIBPISMNAME}
	${CXXLINKER} ${PCC_OBJS} ${ICE_LIB_FLAGS} -o $@

